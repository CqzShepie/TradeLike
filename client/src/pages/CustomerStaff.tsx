import { useEffect, useMemo, useState } from "react";
import Sidebar from "../components/layout/Sidebar";
import { customerStaffService } from "../services/customerStaffService";
import type { CustomerStaffMember, CustomerStaffWorkspace, CustomerTeam } from "../services/customerStaffService";
import { getTeamColour, getTeamColourLabel, teamColours } from "../utils/teamColours";
import { formatPhone, formatShortDate } from "../utils/inputFormatters";

const blankWorkspace: CustomerStaffWorkspace = { teams: [], members: [], entitlements: { planName: "Solo", maxUsers: 1, teamsEnabled: false, staffSchedulingEnabled: false, advancedPermissionsEnabled: false, reportingEnabled: false, apiAccessEnabled: false, supportLevel: "Email" }, roleOptions: [], futureSecurityItems: [], qualityOfLifeItems: [] };
const statuses: CustomerStaffMember["status"][] = ["InvitePending", "Active", "Suspended", "Left"];
type Tab = "staff" | "teams" | "leave" | "future";
type LeaveStatus = "Pending" | "Approved" | "Rejected";
type LeaveRequest = { id: number; staffMemberId: number; startDate: string; endDate: string; reason: string; status: LeaveStatus; createdAt: string };
const leaveStorageKey = "tradelike_staff_leave_requests";

export default function CustomerStaff() {
  const [workspace, setWorkspace] = useState<CustomerStaffWorkspace>(blankWorkspace);
  const [activeTab, setActiveTab] = useState<Tab>("staff");
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [search, setSearch] = useState("");
  const [selectedMemberId, setSelectedMemberId] = useState<number | null>(null);
  const [selectedTeamId, setSelectedTeamId] = useState<number | null>(null);
  const [editingTeamId, setEditingTeamId] = useState<number | null>(null);
  const [teamName, setTeamName] = useState("");
  const [teamDescription, setTeamDescription] = useState("");
  const [teamColour, setTeamColour] = useState("blue");
  const [teamDefaultJobType, setTeamDefaultJobType] = useState("");
  const [teamServiceArea, setTeamServiceArea] = useState("");
  const [teamWorkingHours, setTeamWorkingHours] = useState("");
  const [memberFirstName, setMemberFirstName] = useState("");
  const [memberLastName, setMemberLastName] = useState("");
  const [memberEmail, setMemberEmail] = useState("");
  const [memberPhone, setMemberPhone] = useState("");
  const [memberRole, setMemberRole] = useState("Engineer");
  const [memberTeamIds, setMemberTeamIds] = useState<number[]>([]);
  const [memberSkills, setMemberSkills] = useState("");
  const [memberServiceArea, setMemberServiceArea] = useState("");
  const [memberWorkingHours, setMemberWorkingHours] = useState("");
  const [memberColour, setMemberColour] = useState("blue");
  const [leaveRequests, setLeaveRequests] = useState<LeaveRequest[]>([]);
  const [leaveStaffId, setLeaveStaffId] = useState("");
  const [leaveStartDate, setLeaveStartDate] = useState("");
  const [leaveEndDate, setLeaveEndDate] = useState("");
  const [leaveReason, setLeaveReason] = useState("");

  useEffect(() => { loadWorkspace(); setLeaveRequests(loadLeaveRequests()); }, []);

  const filteredMembers = useMemo(() => { const query = search.trim().toLowerCase(); if (!query) return workspace.members; return workspace.members.filter(member => [member.firstName, member.lastName, member.email, member.roleName, member.status, member.skills, member.serviceArea].join(" ").toLowerCase().includes(query)); }, [workspace.members, search]);
  const currentUserCount = workspace.members.filter(member => member.status !== "Left").length;
  const maxUsers = workspace.entitlements.maxUsers;
  const userLimitReached = maxUsers != null && currentUserCount >= maxUsers;
  const selectedMember = selectedMemberId ? workspace.members.find(member => member.id === selectedMemberId) : null;
  const selectedTeam = selectedTeamId ? workspace.teams.find(team => team.id === selectedTeamId) : null;

  async function loadWorkspace() { try { setLoading(true); setError(""); setWorkspace(await customerStaffService.getWorkspace()); } catch (err) { setError(getErrorMessage(err, "Unable to load staff and teams.")); } finally { setLoading(false); } }
  function clearTeamForm() { setEditingTeamId(null); setTeamName(""); setTeamDescription(""); setTeamColour("blue"); setTeamDefaultJobType(""); setTeamServiceArea(""); setTeamWorkingHours(""); }
  function startEditTeam(team: CustomerTeam) { setEditingTeamId(team.id); setTeamName(team.name); setTeamDescription(team.description); setTeamColour(team.colour || "blue"); setTeamDefaultJobType(team.defaultJobType); setTeamServiceArea(team.serviceArea); setTeamWorkingHours(team.workingHours); setActiveTab("teams"); setSelectedTeamId(team.id); setError(""); setMessage(`Editing ${team.name}.`); }

  async function saveTeam() {
    if (!workspace.entitlements.teamsEnabled) { setError("Upgrade to Team or above to create and edit teams."); return; }
    if (teamName.trim() === "") { setError("Team name is required."); return; }
    try { setSaving(true); setError(""); const request = { name: teamName, description: teamDescription, colour: teamColour, defaultJobType: teamDefaultJobType, serviceArea: teamServiceArea, workingHours: teamWorkingHours }; const updatedWorkspace = editingTeamId ? await customerStaffService.updateTeam(editingTeamId, request) : await customerStaffService.createTeam(request); setWorkspace(updatedWorkspace); setMessage(editingTeamId ? "Team updated." : "Team created."); clearTeamForm(); } catch (err) { setError(getErrorMessage(err, editingTeamId ? "Unable to update team." : "Unable to create team.")); } finally { setSaving(false); }
  }

  async function inviteMember() {
    if (userLimitReached) { setError(`Your ${workspace.entitlements.planName} plan allows ${maxUsers} user${maxUsers === 1 ? "" : "s"}. Upgrade to invite more staff.`); return; }
    if (memberFirstName.trim() === "" || memberLastName.trim() === "" || !memberEmail.includes("@")) { setError("First name, last name and a valid email are required."); return; }
    try { setSaving(true); setError(""); const response = await customerStaffService.createMember({ firstName: memberFirstName, lastName: memberLastName, email: memberEmail, phone: formatPhone(memberPhone), roleName: memberRole, permissionPresetName: memberRole, teamIds: memberTeamIds, skills: memberSkills, serviceArea: memberServiceArea, workingHours: memberWorkingHours, calendarColour: memberColour }); setWorkspace(response.workspace); setMemberFirstName(""); setMemberLastName(""); setMemberEmail(""); setMemberPhone(""); setMemberTeamIds([]); setMemberSkills(""); setMemberServiceArea(""); setMemberWorkingHours(""); setMessage(`Invite created. Link: ${response.inviteLink}`); } catch (err) { setError(getErrorMessage(err, "Unable to invite staff member.")); } finally { setSaving(false); }
  }

  async function updateMember(member: CustomerStaffMember, patch: Partial<CustomerStaffMember>) {
    try { setSaving(true); setError(""); const updated = { ...member, ...patch }; setWorkspace(await customerStaffService.updateMember(member.id, { firstName: updated.firstName, lastName: updated.lastName, email: updated.email, phone: formatPhone(updated.phone), roleName: updated.roleName, status: updated.status, permissionPresetName: updated.permissionPresetName, teamIds: updated.teamIds, skills: updated.skills, serviceArea: updated.serviceArea, workingHours: updated.workingHours, calendarColour: updated.calendarColour, isTwoFactorRequired: updated.isTwoFactorRequired })); setMessage("Staff member updated."); } catch (err) { setError(getErrorMessage(err, "Unable to update staff member.")); } finally { setSaving(false); }
  }

  async function requestPasswordReset(member: CustomerStaffMember) { try { setSaving(true); setError(""); setWorkspace(await customerStaffService.requestPasswordReset(member.id)); setMessage(`Password reset request recorded for ${member.email}.`); } catch (err) { setError(getErrorMessage(err, "Unable to request password reset.")); } finally { setSaving(false); } }
  async function deleteTeam(team: CustomerTeam) { if (!confirm(`Delete ${team.name}? Staff will remain but the team grouping will be removed.`)) return; try { setSaving(true); setError(""); setWorkspace(await customerStaffService.deleteTeam(team.id)); if (editingTeamId === team.id) clearTeamForm(); if (selectedTeamId === team.id) setSelectedTeamId(null); setMessage("Team deleted."); } catch (err) { setError(getErrorMessage(err, "Unable to delete team.")); } finally { setSaving(false); } }

  function addLeaveRequest() {
    const staffId = Number(leaveStaffId || selectedMemberId || 0);
    if (!staffId || !leaveStartDate || !leaveEndDate) { setError("Choose a staff member and leave dates."); return; }
    const request: LeaveRequest = { id: Date.now(), staffMemberId: staffId, startDate: leaveStartDate, endDate: leaveEndDate, reason: leaveReason.trim(), status: "Pending", createdAt: new Date().toISOString() };
    const next = [request, ...leaveRequests]; saveLeaveRequests(next); setLeaveRequests(next); setLeaveStaffId(""); setLeaveStartDate(""); setLeaveEndDate(""); setLeaveReason(""); setMessage("Annual leave request added.");
  }
  function updateLeaveStatus(id: number, status: LeaveStatus) { const next = leaveRequests.map(request => request.id === id ? { ...request, status } : request); saveLeaveRequests(next); setLeaveRequests(next); }

  return <main className="flex min-h-screen bg-slate-50"><Sidebar /><section className="min-w-0 flex-1 p-10"><div className="max-w-7xl"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-wide text-blue-600">Customer staff</p><h1 className="mt-1 text-3xl font-bold text-slate-900">Staff & Teams</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Invite workers, build flexible teams, assign roles, plan job calendars, and keep customer company staff completely separate from TradeLike internal staff.</p></div><div className="rounded-xl border border-slate-200 bg-white p-4 text-sm shadow-sm"><p className="font-bold text-slate-900">{workspace.entitlements.planName} plan</p><p className="mt-1 text-slate-600">{currentUserCount}/{maxUsers ?? "âˆž"} users</p><p className="text-slate-600">Support: {workspace.entitlements.supportLevel}</p></div></div>{error && <Alert tone="error" onClose={() => setError("")}>{error}</Alert>}{message && <Alert tone="success" onClose={() => setMessage("")}>{message}</Alert>}<div className="mt-8 grid gap-3 md:grid-cols-4"><TabButton active={activeTab === "staff"} onClick={() => setActiveTab("staff")}>Staff</TabButton><TabButton active={activeTab === "teams"} onClick={() => setActiveTab("teams")}>Teams</TabButton><TabButton active={activeTab === "leave"} onClick={() => setActiveTab("leave")}>Annual leave</TabButton><TabButton active={activeTab === "future"} onClick={() => setActiveTab("future")}>Future QOL</TabButton></div>{loading ? <p className="mt-8 text-sm text-slate-500">Loading...</p> : <div className="mt-6">{activeTab === "staff" && <div className="grid gap-6 xl:grid-cols-[380px_minmax(0,1fr)]"><Panel title="Invite worker">{userLimitReached && <div className="mb-4 rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800">User limit reached. Upgrade to add more workers.</div>}<div className="grid gap-3"><Input placeholder="First name" value={memberFirstName} onChange={setMemberFirstName} /><Input placeholder="Last name" value={memberLastName} onChange={setMemberLastName} /><Input placeholder="Email" value={memberEmail} onChange={setMemberEmail} /><Input placeholder="Phone" value={memberPhone} onChange={value => setMemberPhone(formatPhone(value))} /><Select value={memberRole} onChange={setMemberRole}>{workspace.roleOptions.map(role => <option key={role} value={role}>{role}</option>)}</Select><TeamPicker teams={workspace.teams} selectedIds={memberTeamIds} onChange={setMemberTeamIds} /><Input placeholder="Skills e.g. gas safe, installs, servicing" value={memberSkills} onChange={setMemberSkills} /><Input placeholder="Service area / postcode coverage" value={memberServiceArea} onChange={setMemberServiceArea} /><Input placeholder="Working hours e.g. Mon-Fri 8-5" value={memberWorkingHours} onChange={setMemberWorkingHours} /><Select value={memberColour} onChange={setMemberColour}>{teamColours.map(colour => <option key={colour.value} value={colour.value}>{colour.label}</option>)}</Select><button type="button" disabled={saving || userLimitReached} onClick={inviteMember} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700 disabled:cursor-not-allowed disabled:bg-slate-300">{saving ? "Saving..." : "Invite worker"}</button></div></Panel><Panel title="Company staff"><Input placeholder="Search staff, role, skills, area..." value={search} onChange={setSearch} /><div className="mt-4 grid gap-3">{filteredMembers.map(member => <StaffCard key={member.id} member={member} teams={workspace.teams} leaveRequests={leaveRequests} selected={selectedMemberId === member.id} onSelect={() => setSelectedMemberId(selectedMemberId === member.id ? null : member.id)} onPasswordReset={() => requestPasswordReset(member)} onStatusChange={status => updateMember(member, { status })} onRoleChange={role => updateMember(member, { roleName: role, permissionPresetName: role })} onTeamsChange={teamIds => updateMember(member, { teamIds })} roleOptions={workspace.roleOptions} />)}{filteredMembers.length === 0 && <p className="text-sm text-slate-500">No staff found.</p>}</div>{selectedMember && <DetailBox title={`${selectedMember.firstName} ${selectedMember.lastName}`} items={[`Role: ${selectedMember.roleName}`, `Status: ${selectedMember.status}`, `Email: ${selectedMember.email}`, `Phone: ${selectedMember.phone || "Not set"}`, `Working hours: ${selectedMember.workingHours || "Not set"}`, `Upcoming leave: ${formatLeaveSummary(nextLeaveForMember(leaveRequests, selectedMember.id))}`]} />}</Panel></div>}{activeTab === "teams" && <div className="grid gap-6 xl:grid-cols-[380px_minmax(0,1fr)]"><Panel title={editingTeamId ? "Edit team" : "Create team"}>{!workspace.entitlements.teamsEnabled && <div className="mb-4 rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800">Teams unlock on Team, Business, and Enterprise.</div>}{editingTeamId && <div className="mb-4 rounded-lg border border-blue-200 bg-blue-50 p-3 text-sm font-semibold text-blue-800">Editing an existing team. Save to update it or cancel to create a new one.</div>}<div className="grid gap-3"><Input placeholder="Team name" value={teamName} onChange={setTeamName} /><Input placeholder="Description" value={teamDescription} onChange={setTeamDescription} /><Select value={teamColour} onChange={setTeamColour}>{teamColours.map(colour => <option key={colour.value} value={colour.value}>{colour.label}</option>)}</Select><Input placeholder="Default job type" value={teamDefaultJobType} onChange={setTeamDefaultJobType} /><Input placeholder="Service area" value={teamServiceArea} onChange={setTeamServiceArea} /><Input placeholder="Working hours" value={teamWorkingHours} onChange={setTeamWorkingHours} /><button type="button" disabled={saving || !workspace.entitlements.teamsEnabled} onClick={saveTeam} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700 disabled:cursor-not-allowed disabled:bg-slate-300">{saving ? "Saving..." : editingTeamId ? "Save team" : "Create team"}</button>{editingTeamId && <button type="button" onClick={clearTeamForm} className="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancel edit</button>}</div></Panel><Panel title="Teams"><div className="grid gap-3 md:grid-cols-2">{workspace.teams.map(team => <TeamCard key={team.id} team={team} members={workspace.members.filter(member => member.teamIds.includes(team.id))} selected={selectedTeamId === team.id} onSelect={() => setSelectedTeamId(selectedTeamId === team.id ? null : team.id)} onDelete={() => deleteTeam(team)} onEdit={() => startEditTeam(team)} />)}</div>{selectedTeam && <DetailBox title={selectedTeam.name} items={[`Colour: ${getTeamColourLabel(selectedTeam.colour)}`, `Area: ${selectedTeam.serviceArea || "Not set"}`, `Hours: ${selectedTeam.workingHours || "Not set"}`, `Default job type: ${selectedTeam.defaultJobType || "Not set"}`, `Members: ${workspace.members.filter(member => member.teamIds.includes(selectedTeam.id)).map(member => `${member.firstName} ${member.lastName}`).join(", ") || "None"}`]} />}</Panel></div>}{activeTab === "leave" && <Panel title="Annual leave"><div className="grid gap-3 md:grid-cols-5"><select value={leaveStaffId || String(selectedMemberId ?? "")} onChange={event => setLeaveStaffId(event.target.value)} className="rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none focus:border-blue-600"><option value="">Choose staff</option>{workspace.members.filter(member => member.status !== "Left").map(member => <option key={member.id} value={member.id}>{member.firstName} {member.lastName}</option>)}</select><Input type="date" placeholder="Start" value={leaveStartDate} onChange={setLeaveStartDate} /><Input type="date" placeholder="End" value={leaveEndDate} onChange={setLeaveEndDate} /><Input placeholder="Reason" value={leaveReason} onChange={setLeaveReason} /><button type="button" onClick={addLeaveRequest} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Add leave</button></div><div className="mt-5 grid gap-3">{leaveRequests.map(request => { const member = workspace.members.find(item => item.id === request.staffMemberId); return <div key={request.id} className="rounded-lg border border-slate-200 bg-slate-50 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold text-slate-900">{member ? `${member.firstName} ${member.lastName}` : "Unknown staff"}</p><p className="text-sm text-slate-600">{formatShortDate(request.startDate)} â†’ {formatShortDate(request.endDate)} Â· {request.reason || "Annual leave"}</p></div><Select value={request.status} onChange={value => updateLeaveStatus(request.id, value as LeaveStatus)}><option value="Pending">Pending</option><option value="Approved">Approved</option><option value="Rejected">Rejected</option></Select></div></div>; })}{leaveRequests.length === 0 && <p className="rounded-lg border border-dashed border-slate-300 bg-slate-50 p-4 text-sm text-slate-500">No annual leave booked yet.</p>}</div></Panel>}{activeTab === "future" && <Panel title="Planned quality-of-life and security"><div className="grid gap-3 md:grid-cols-2">{[...workspace.qualityOfLifeItems, ...workspace.futureSecurityItems].map(item => <div key={item} className="rounded-lg border border-slate-200 bg-slate-50 p-4 text-sm font-medium text-slate-700">{item}</div>)}</div></Panel>}</div>}</div></section></main>;
}

function StaffCard({ member, teams, leaveRequests, selected, onSelect, onPasswordReset, onStatusChange, onRoleChange, onTeamsChange, roleOptions }: { member: CustomerStaffMember; teams: CustomerTeam[]; leaveRequests: LeaveRequest[]; selected: boolean; onSelect: () => void; onPasswordReset: () => void; onStatusChange: (status: CustomerStaffMember["status"]) => void; onRoleChange: (role: string) => void; onTeamsChange: (ids: number[]) => void; roleOptions: string[] }) { const leave = nextLeaveForMember(leaveRequests, member.id); return <div onClick={onSelect} className={`cursor-pointer rounded-xl border p-4 transition ${selected ? "border-blue-500 bg-blue-50" : "border-slate-200 bg-slate-50 hover:bg-white"}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold text-slate-900">{member.firstName} {member.lastName}</p><p className="text-sm text-slate-600">{member.email}</p><p className="mt-1 text-xs font-semibold text-blue-700">{member.roleName} Â· {member.status}</p>{leave && <p className="mt-1 text-xs font-semibold text-amber-700">Leave: {formatShortDate(leave.startDate)} â†’ {formatShortDate(leave.endDate)} ({leave.status})</p>}</div><div className="flex flex-wrap gap-2" onClick={event => event.stopPropagation()}><button type="button" onClick={onPasswordReset} className="rounded-lg border border-slate-300 px-3 py-1 text-xs font-semibold text-slate-700 hover:bg-white">Reset password</button><Select value={member.status} onChange={value => onStatusChange(value as CustomerStaffMember["status"])}>{statuses.map(status => <option key={status} value={status}>{status}</option>)}</Select></div></div><div className="mt-3 grid gap-2 md:grid-cols-2" onClick={event => event.stopPropagation()}><Select value={member.roleName} onChange={onRoleChange}>{roleOptions.map(role => <option key={role} value={role}>{role}</option>)}</Select><TeamPicker teams={teams} selectedIds={member.teamIds} onChange={onTeamsChange} compact /></div><div className="mt-3 grid gap-2 text-xs text-slate-600 md:grid-cols-3"><span>Skills: {member.skills || "Not set"}</span><span>Area: {member.serviceArea || "Not set"}</span><span>Reset: {member.resetPasswordRequestedAt ? formatShortDate(member.resetPasswordRequestedAt) : "No request"}</span></div></div>; }
function TeamCard({ team, members, selected, onSelect, onDelete, onEdit }: { team: CustomerTeam; members: CustomerStaffMember[]; selected: boolean; onSelect: () => void; onDelete: () => void; onEdit: () => void }) { const colour = getTeamColour(team.colour); return <div onClick={onSelect} className={`cursor-pointer rounded-xl border p-4 transition ${selected ? "ring-2 ring-blue-400" : ""}`} style={{ borderColor: colour.hex, backgroundColor: colour.softHex }}><div className="flex justify-between gap-4"><div><p className="font-bold text-slate-900">{team.name}</p><p className="text-sm text-slate-600">{team.description || "No description"}</p><p className="mt-1 text-xs font-bold" style={{ color: colour.hex }}>{colour.label}</p></div><div className="flex flex-col gap-2" onClick={event => event.stopPropagation()}><button type="button" onClick={onDelete} className="h-fit rounded border border-red-200 bg-white px-2 py-1 text-xs font-semibold text-red-600 hover:bg-red-50">Delete</button><button type="button" onClick={onEdit} className="h-fit rounded border border-slate-300 bg-white px-2 py-1 text-xs font-semibold text-slate-700 hover:bg-slate-50">Edit</button></div></div><div className="mt-3 text-xs text-slate-600"><p>{members.length} member{members.length === 1 ? "" : "s"}</p><p>Area: {team.serviceArea || "Not set"}</p><p>Hours: {team.workingHours || "Not set"}</p></div></div>; }
function DetailBox({ title, items }: { title: string; items: string[] }) { return <div className="mt-4 rounded-xl border border-blue-100 bg-blue-50 p-4"><p className="font-bold text-slate-900">{title}</p><div className="mt-2 grid gap-1 text-sm text-slate-700">{items.map(item => <span key={item}>{item}</span>)}</div></div>; }
function TeamPicker({ teams, selectedIds, onChange, compact = false }: { teams: CustomerTeam[]; selectedIds: number[]; onChange: (ids: number[]) => void; compact?: boolean }) { if (teams.length === 0) return <p className="rounded-lg border border-slate-200 bg-slate-50 p-3 text-sm text-slate-500">No teams yet.</p>; return <div className={`flex flex-wrap gap-2 ${compact ? "" : "rounded-lg border border-slate-200 p-3"}`}>{teams.map(team => <button key={team.id} type="button" onClick={() => onChange(selectedIds.includes(team.id) ? selectedIds.filter(id => id !== team.id) : [...selectedIds, team.id])} className={`rounded-full border px-3 py-1 text-xs font-semibold ${selectedIds.includes(team.id) ? "border-blue-600 bg-blue-600 text-white" : "border-slate-300 bg-white text-slate-700 hover:bg-slate-50"}`}>{team.name}</button>)}</div>; }
function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold text-slate-900">{title}</h2><div className="mt-4">{children}</div></section>; }
function TabButton({ active, onClick, children }: { active: boolean; onClick: () => void; children: React.ReactNode }) { return <button type="button" onClick={onClick} className={`rounded-xl border p-4 text-left text-sm font-bold ${active ? "border-blue-600 bg-blue-600 text-white" : "border-slate-200 bg-white text-slate-700 hover:bg-slate-50"}`}>{children}</button>; }
function Alert({ tone, children, onClose }: { tone: "error" | "success"; children: React.ReactNode; onClose: () => void }) { return <div className={`mt-6 flex items-start justify-between gap-4 rounded-xl border p-4 text-sm font-semibold ${tone === "error" ? "border-red-200 bg-red-50 text-red-700" : "border-green-200 bg-green-50 text-green-700"}`}><span>{children}</span><button type="button" onClick={onClose} className="rounded px-2 text-lg leading-none hover:bg-white/70">Ã—</button></div>; }
function Input({ value, onChange, placeholder, type = "text" }: { value: string; onChange: (value: string) => void; placeholder: string; type?: string }) { return <input type={type} value={value} onChange={event => onChange(event.target.value)} placeholder={placeholder} className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none focus:border-blue-600" />; }
function Select({ value, onChange, children }: { value: string; onChange: (value: string) => void; children: React.ReactNode }) { return <select value={value} onChange={event => onChange(event.target.value)} className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none focus:border-blue-600">{children}</select>; }
function loadLeaveRequests() { try { return JSON.parse(localStorage.getItem(leaveStorageKey) ?? "[]") as LeaveRequest[]; } catch { return []; } }
function saveLeaveRequests(requests: LeaveRequest[]) { localStorage.setItem(leaveStorageKey, JSON.stringify(requests)); }
function nextLeaveForMember(requests: LeaveRequest[], staffMemberId: number) { const now = new Date(); now.setHours(0, 0, 0, 0); return requests.filter(request => request.staffMemberId === staffMemberId && request.status !== "Rejected" && new Date(request.endDate) >= now).sort((a, b) => new Date(a.startDate).getTime() - new Date(b.startDate).getTime())[0]; }
function formatLeaveSummary(request?: LeaveRequest) { return request ? `${formatShortDate(request.startDate)} â†’ ${formatShortDate(request.endDate)} (${request.status})` : "None"; }
function getErrorMessage(error: unknown, fallback: string) { return error instanceof Error && error.message.trim() !== "" ? error.message : fallback; }


